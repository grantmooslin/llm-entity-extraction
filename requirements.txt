# llm-entity-extraction — CORE runtime dependencies only (KANBAN-081).
# Python 3.10+ (tested on 3.13).
#
# This is the minimal floor for the agent -> prompt -> scoring chain
# (llm-mailroom imports exactly this surface). Install task-specific
# batches SEPARATELY so you never pull more than your operative needs:
#
#     pip install -r requirements/tracing.txt     # Langfuse + Arize Phoenix observability
#     pip install -r requirements/evals.txt       # Braintrust dataset streaming / eval harness
#     pip install -r requirements/datasets.txt    # HF Hub publishers + PDF/vision ingestion
#     pip install -r requirements/reporting.txt   # decks, EDA plots, xlsx exports
#     pip install -r requirements/embeddings.txt  # local embedding rescue (pulls torch ~2-3 GB)
#     pip install -r requirements/all.txt         # everything non-dev
#     pip install -r requirements/dev.txt         # test suite
#
# Equivalently via package extras: pip install -e ".[tracing]" etc.
# Membership is pinned by tests/test_dependency_manifests.py.

# LLM framework
langchain-core>=1.0
langchain-openai>=0.3
openai>=1.60

# Provider calls + config
requests>=2.32.0
python-dotenv>=1.0.0

# Config / logging
PyYAML>=6.0
structlog>=24.0

# Scoring library shared with llm-mailroom (pinned v0.10.0 — single source of
# scoring definitions across both repos)
llm-dojo-scoring @ git+https://github.com/Exios66/llm-dojo-scoring.git@v0.10.0
